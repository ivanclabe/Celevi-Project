const assert = require('assert');
// Proceso de interpolacion de sentencias. Toma la estructura
// captada por el socket y la lleva a un formato mas legible
// para el modulo 'GPS'.

// eslint-disable-next-line no-irregular-whitespace
module.exports = sentence => {
  assert(typeof sentence === 'string');
};
